#include <stdio.h>

int main(void)
{
    /*
    int man;
    int woman;
    int child;
    for(man = 1; man <= 28; man++)
    {
        for(woman = 1; woman <= 28; woman++)
        {
            child = 30-man-woman;
            
                if(man*3+woman*2+child == 50)
                {
                    printf("男人有%d个，女人有%d个，小孩有%d个。\n", man,woman,child);
                }
            
        }
    }
    */

    for(int man = 1;man <= 15; man++)
    {
        for(int woman = 1; woman <= 23; woman++)
        {
            int child = 30 - man - woman;
            int money = man*3 + woman*2 + child;
            if(money == 50)
            {
                printf("可能的结果：男人%d个,女人%d个,小孩%d个.\n",man,woman,child);
            }
        }
    }
    
    return 0;
}
