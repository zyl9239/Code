#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char* argv[])
{
    //char *names[5] = {"mingyang","dongtao","wangfeng"};
    
    for(int i = 0; i < argc; i++)
    {
        printf("argv[%d] >>>> %s\n",i,argv[i]);

    }

    sleep(960);
    return 0;
}
